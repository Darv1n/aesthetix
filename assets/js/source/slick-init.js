/*
** Scripts within the all post window for slick init.
*/

jQuery( document ).ready( function ( $ ) {

	$( '.slick-gallery-slider' ).slick( {
		dots: true,
		arrows: false,
		adaptiveHeight: true,
		// autoplay: true,
		// autoplaySpeed: 3000,
	} );

} );
